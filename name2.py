name = input("What is your name? ")
print("hello,", name) 
# This version combines 2 separate inputs to print - any number of arguments/inputs can be added, just need to separate them with commas
# Default behavior of 'print' function: separates inputs with a space - so the space we added in front of the closing " can be removed